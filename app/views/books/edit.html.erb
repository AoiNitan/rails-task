<h1>Editing Book</h1>

<%= form_with model: @book do |f| %>


  <p>title</p>
  <%= f.text_field :title %>

  <p>Body</p>
  <%= f.text_area :body %>

  <%= f.submit 'Update Book' %>
<% end %>

<%= link_to 'Show',book_path(@book.id) %>
<p>|</p>
<%= link_to 'Back','/books' %>
